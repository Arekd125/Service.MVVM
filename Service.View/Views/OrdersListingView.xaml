<UserControl
    x:Class="Service.View.Views.OrdersListingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Service.View.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <DataGrid
        Grid.Column="1"
        Margin="10"
        AutoGenerateColumns="False"
        BorderThickness="1"
        CanUserAddRows="False"
        IsReadOnly="True"
        ItemsSource="{Binding ordersViewModel}"
        SelectionMode="Single">

        <DataGrid.Columns>
            <DataGridTextColumn Binding="{Binding Path=OrderName}" Header="nr zlecenie" />
            <DataGridTextColumn Binding="{Binding Path=StartData}" Header="Data" />
            <DataGridTextColumn Binding="{Binding Path=ContactName}" Header="Imię i nazwisko" />
            <DataGridTextColumn Binding="{Binding Path=ContactPhoneNumber}" Header="telefon" />
            <DataGridTextColumn Binding="{Binding Path=DeviceName}" Header="marka" />
            <DataGridTextColumn Binding="{Binding Path=DeviceModelName}" Header="model" />
        </DataGrid.Columns>
    </DataGrid>
</UserControl>