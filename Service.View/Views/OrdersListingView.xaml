<UserControl
    x:Class="Service.View.Views.OrdersListingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:conv="clr-namespace:Service.View.Converters"
    xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xml:lang="pl-PL"
    Dialog:DialogParticipation.Register="{Binding}"
    d:DesignHeight="450"
    d:DesignWidth="800"

    mc:Ignorable="d">
    <UserControl.Resources>
        <conv:StringNullOrEmptyToVisibilityConverter x:Key="stringNullOrEmptyToVisibilityConverter" />
        <conv:EmptyToDoVisibiltyConverter x:Key="emptyToDoVisibiltyConverter" />
    </UserControl.Resources>
    <DataGrid
        HorizontalAlignment="Left"
        Margin="0,10,0,0"
        AutoGenerateColumns="False"
        BorderThickness="1"
        CanUserAddRows="False"
        GridLinesVisibility="All"
        IsReadOnly="True"
        ItemsSource="{Binding OrdersViewModelCollection}"
        SelectedIndex="{Binding OrdersViewModelSelectedIndex}"
        SelectionMode="Single">

        <DataGrid.Resources>
            <Style TargetType="{x:Type DataGrid}" BasedOn="{StaticResource MahApps.Styles.DataGrid}">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="SelectedItem" Value="Null" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </DataGrid.Resources>

        <DataGrid.Columns>
            <DataGridTextColumn Binding="{Binding Path=OrderName}" Header="nr zlecenie" />
            <DataGridTextColumn Binding="{Binding Path=StartDate}" Header="Data" />
            <DataGridTextColumn Binding="{Binding Path=ContactName}" Header="Imię i nazwisko" />
            <DataGridTextColumn Binding="{Binding Path=ContactPhoneNumber}" Header="telefon" />
            <DataGridTextColumn Binding="{Binding Path=Device}" Header="marka" />
            <DataGridTextColumn Binding="{Binding Path=Model}" Header="model" />

            <!--STATUS -->

            <DataGridTemplateColumn Header="Status">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <ToggleButton IsChecked="{Binding IsFinished}" HorizontalAlignment="Stretch" Foreground="White"
                                      Unchecked="ToggleButton_Unchecked"
                                      Checked="ToggleButton_Checked"
                                        Command="{Binding DataContext.EditStatusButton, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                            <ToggleButton.Style>
                                <Style TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource MahApps.Styles.Button.Win10}">
                                    <Style.Triggers>
                                        <Trigger Property="IsChecked" Value="False">
                                            <Setter Property="Content" Value="Otwarte"></Setter>
                                            <Setter Property="Background" Value="{StaticResource MahApps.Brushes.Accent}"></Setter>
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter Property="Content" Value="Zakończone"></Setter>
                                            <Setter Property="Background" Value="Green"></Setter>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ToggleButton.Style>
                        </ToggleButton>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <!--  Buttons  -->
            <DataGridTemplateColumn  Header="Edycja">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Button
                                Margin="2,0,2,0"
                                mah:ControlsHelper.ContentCharacterCasing="Upper"
                                Command="{Binding DataContext.EditOrderButton, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                Style="{StaticResource MahApps.Styles.Button.Square.Accent}">
                                <mah:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE70F;" />
                            </Button>
                            <Button
                                Margin="2,0,2,0"
                                mah:ControlsHelper.ContentCharacterCasing="Upper"
                                Background="Red"
                                Command="{Binding DataContext.DeleteOrderButton, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                Style="{StaticResource MahApps.Styles.Button.Square.Accent}">
                                <mah:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8BB;" />
                            </Button>
                        </StackPanel>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
        </DataGrid.Columns>
        <DataGrid.RowDetailsTemplate>

            <!--  RowDetails  -->
            <DataTemplate>
                <Grid Background="White">
                    <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                        <!--  KOSZT  -->
                        <StackPanel Margin="10,5,10,5" Visibility="{Binding Path=ToDo, Converter={StaticResource emptyToDoVisibiltyConverter}}">
                            <WrapPanel>
                                <TextBlock

                                    FontWeight="Bold"
                                    Foreground="Black"
                                    Text="Usługi: " />
                            </WrapPanel>
                            <!--  USŁUGI  -->

                            <DataGrid
                                Width="auto"
                                AutoGenerateColumns="False"
                                CanUserDeleteRows="False"
                                GridLinesVisibility="Horizontal"
                                HeadersVisibility="None"
                                IsEnabled="False"
                                ItemsSource="{Binding Path=ToDo, UpdateSourceTrigger=PropertyChanged}"
                                SelectionMode="Single"
                                Visibility="{Binding Path=ToDo, Converter={StaticResource emptyToDoVisibiltyConverter}}">

                                <DataGrid.Columns>
                                    <DataGridTextColumn
                                        Width="auto"
                                        Binding="{Binding Path=ToDoName}"
                                        Foreground="Black"
                                        Header="Usługi" />
                                    <DataGridTextColumn
                                        Width="80"
                                        Binding="{Binding Path=Prize, UpdateSourceTrigger=PropertyChanged, StringFormat=\{0:C2\}}"
                                        Foreground="Black"
                                        Header="Cena">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right" />
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                            <TextBlock Margin="5" Foreground="Black" FontWeight="Bold"  Text="{Binding Path=Cost, StringFormat=Szacowany Koszt: \{0:C2\}}" />
                        </StackPanel>

                        <!--  OPIS USTERKI  -->
                        <StackPanel Margin="10,5,10,5" Visibility="{Binding Path=Description, Converter={StaticResource stringNullOrEmptyToVisibilityConverter}}">
                            <TextBlock
                                FontWeight="Bold"
                                Foreground="Black"
                                Text="Opis usterki: " />
                            <TextBlock Foreground="Black" Text="{Binding Path=Description, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <!--  AKCESORIA  -->
                        <StackPanel Margin="10,5,10,5" Visibility="{Binding Accessories, Converter={StaticResource stringNullOrEmptyToVisibilityConverter}}">
                            <TextBlock
                                FontWeight="Bold"
                                Foreground="Black"
                                Text="Dołączone akcesoria: " />
                            <TextBlock Foreground="Black" Text="{Binding Accessories}" />
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </DataTemplate>
        </DataGrid.RowDetailsTemplate>
    </DataGrid>
</UserControl>